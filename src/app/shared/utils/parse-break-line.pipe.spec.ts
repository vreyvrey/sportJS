import { ParseBreakLinePipe } from './parse-break-line.pipe';

describe('ParseBreakLinePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseBreakLinePipe();
    expect(pipe).toBeTruthy();
  });
});
